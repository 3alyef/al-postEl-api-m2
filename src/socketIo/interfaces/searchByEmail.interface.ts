export interface MessageUserResponse {
    found: boolean,
    userSoul: string | null, 
    message: string 
}

export interface Message {
    found: boolean,
    userSoul: string | null, 
    message: string 
}